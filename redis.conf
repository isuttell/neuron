# Enable keyspace notifications for expired events and key operations
notify-keyspace-events KEx

# Persistence configuration
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Connection
timeout 0
tcp-keepalive 300

# Logging
loglevel notice
