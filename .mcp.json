{
    "mcpServers": {
        "gitea": {
            "command": "gitea-mcp",
            "args": [
                "-t",
                "sse",
                "-port",
                "8080",
                "--host",
                "https://gitea.zaks.io"
            ],
            "env": {
                "GITEA_ACCESS_TOKEN": "${GITEA_ACCESS_TOKEN}"
            }
        }
    }
}