name: Claude PR Review

on:
  pull_request:
    branches: [ master ]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Download and setup gitea-mcp
        run: |
          wget https://gitea.com/gitea/gitea-mcp/releases/download/v0.1.9/gitea-mcp_Linux_x86_64.tar.gz
          # Extract to /usr/local/bin which is in the default PATH
          sudo tar -xzf gitea-mcp_Linux_x86_64.tar.gz -C /usr/local/bin
          sudo chmod +x /usr/local/bin/gitea-mcp
          # Verify it's accessible
          which gitea-mcp || { echo "ERROR: gitea-mcp not found in PATH"; exit 1; }

      - name: Install Claude CLI
        run: |
          npm install -g @anthropic-ai/claude-code
          # Log Claude version for debugging
          claude --version || echo "Claude version command not available"


      - name: Configure Claude
        env:
          GITEA_ACCESS_TOKEN: ${{ secrets.GT_TOKEN }}
          GITEA_BASE:  ${{ gitea.server_url }}
        run: |
          # Configure the Gitea MCP server using the CLI with SSE transport
          claude mcp add gitea \
            -e GITEA_ACCESS_TOKEN=$GITEA_ACCESS_TOKEN \
            -- gitea-mcp \
            -host $GITEA_BASE \
            -token $GITEA_ACCESS_TOKEN
          claude mcp list
          echo "Configured MCP server for Gitea"

          # Configure allowed tools for Claude
          claude config add allowedTools "Read"
          claude config add allowedTools "Bash(git show:*)"
          claude config add allowedTools "Bash(git log:*)"
          claude config add allowedTools "Bash(git diff:*)"
          claude config add allowedTools "mcp__gitea__create_issue_comment"
          claude config add allowedTools "mcp__gitea__get_pull_request_by_index"

          echo "Configured Claude allowed tools"
          claude config get allowedTools

      - name: Run Claude review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITEA_ACCESS_TOKEN: ${{ secrets.GT_TOKEN }}
          PR_NUMBER: ${{ gitea.event.pull_request.number }}
        run: |
          echo "Fetching PR diff..."
          # Fetch the PR diff from Gitea API
          curl -sSL -H "Authorization: token $GITEA_ACCESS_TOKEN" \
            "${{ gitea.server_url }}/api/v1/repos/${{ gitea.repository_owner }}/${{ gitea.repository_name }}/pulls/$PR_NUMBER.diff" \
            -o pr.diff

          echo "Running code review..."
          PROMPT="Please review the code changes in this PR based on the diff. 

          1. Check for existing comments on the PR #$PR_NUMBER for context
          2. Read related code for additional context so you understand the impact of the diff
          3. Analyze code quality, potential bugs, and improvements of diff
          4. Use Gitea MCP tools to comment directly on PR #$PR_NUMBER
          5. Be specific in your comments and reference line numbers when possible
          6. Focus on problems and potential improvements
          
          Be concise."
          
          # Create input for Claude that includes both prompt and diff
          (echo "$PROMPT"; echo; echo "<git_diff>"; cat pr.diff; echo "</git_diff>") | claude -p --max-turns 25