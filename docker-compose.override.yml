services:
  server:
    env_file:
      - .env
    ports:
      - 5000:5000

  db:
    ports:
      - 5432:5432
    env_file:
      - .env
    volumes:
      - ./initdb.d:/docker-entrypoint-initdb.d
      - database-data:/var/lib/postgresql/data

volumes:
  database-data:
